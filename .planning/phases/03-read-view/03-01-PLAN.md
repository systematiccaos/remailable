---
phase: 03-read-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/account.rs
  - src/storage.rs
  - src/imap_conn.rs
  - src/sync.rs
autonomous: true
requirements: [READ-01, READ-03, READ-04, READ-05, READ-06, READ-07, ATCH-01, ATCH-02, ATCH-03]

must_haves:
  truths:
    - "Email MIME structure can be parsed to extract content type and attachment metadata"
    - "HTML content can be converted to e-ink-friendly plain text"
    - "Emails can be searched by subject or sender in local storage"
    - "Emails can be grouped into threads by In-Reply-To header"
    - "Read/unread status changes propagate to the IMAP server"
    - "Attachment metadata (filename, size, content type) is extracted and stored"
  artifacts:
    - path: "src/account.rs"
      provides: "Extended EmailMetadata with content_type, in_reply_to, thread_id, has_attachments; AttachmentMetadata struct"
      contains: "AttachmentMetadata"
    - path: "src/storage.rs"
      provides: "Search, thread grouping, attachment CRUD, get_email, migration logic"
      contains: "search_emails"
    - path: "src/imap_conn.rs"
      provides: "BODYSTRUCTURE parsing, thread header fetching, attachment download, html_to_eink, IMAP STORE for read flags"
      contains: "html_to_eink"
    - path: "src/sync.rs"
      provides: "Updated sync with content type, threading, and attachment metadata"
  key_links:
    - from: "src/sync.rs"
      to: "src/imap_conn.rs"
      via: "BODYSTRUCTURE fetching and thread header extraction"
      pattern: "fetch_bodystructure"
    - from: "src/storage.rs"
      to: "email_metadata table"
      via: "New columns: content_type, in_reply_to, thread_id, has_attachments"
      pattern: "thread_id"
---

<objective>
ALREADY COMPLETED — This plan was executed in a previous session.

Rust backend extensions: MIME parsing via BODYSTRUCTURE, HTML-to-eink conversion, search, thread grouping, attachment metadata, IMAP flag sync.

Purpose: All Rust backend logic needed by Plans 02 and 03 for the email reading UI.

Output: Extended EmailMetadata, AttachmentMetadata struct, search/thread/attachment Storage methods, html_to_eink converter, BODYSTRUCTURE parsing, IMAP mark_as_seen.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-read-view/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: COMPLETED — Extend data model and storage for reading, threads, search, and attachments</name>
  <files>src/account.rs, src/storage.rs</files>
  <action>Already completed in commit 1f4e393. EmailMetadata extended with content_type, in_reply_to, thread_id, has_attachments. AttachmentMetadata struct added. Storage methods: get_email, search_emails, list_thread, save/list/get/mark_downloaded attachments. Schema migration with PRAGMA table_info. 12 new tests passing.</action>
  <verify>
    <automated>QMAKE=/opt/homebrew/bin/qmake PKG_CONFIG_PATH="" PKG_CONFIG_SYSROOT_DIR="" cargo check 2>&1 | tail -3</automated>
  </verify>
  <done>Data model and storage extensions complete. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: COMPLETED — Extend IMAP for BODYSTRUCTURE, attachment download, HTML conversion; update sync</name>
  <files>src/imap_conn.rs, src/sync.rs, Cargo.toml</files>
  <action>Already completed in commit 964d9d1. IMAP BODYSTRUCTURE regex parsing, fetch_message_headers_for_thread, fetch_attachment_part, html_to_eink converter, calculate_thread_id. Sync engine stores content_type, threading, attachment metadata. 13 new tests passing. Total: 48 tests (5 ignored for live IMAP).</action>
  <verify>
    <automated>QMAKE=/opt/homebrew/bin/qmake PKG_CONFIG_PATH="" PKG_CONFIG_SYSROOT_DIR="" cargo check 2>&1 | tail -3</automated>
  </verify>
  <done>IMAP extensions complete. All tests pass.</done>
</task>

</tasks>

<verification>
1. `cargo check` compiles with local Qt override
2. All 48 unit tests pass (5 ignored for live IMAP)
3. EmailMetadata has content_type, in_reply_to, thread_id, has_attachments fields
4. AttachmentMetadata struct exists with full CRUD
5. search_emails works with case-insensitive LIKE matching
6. html_to_eink strips HTML to plain text for e-ink rendering
</verification>

<success_criteria>
✅ All backend data layer extensions are complete and compiling.
✅ 03-01-SUMMARY.md exists with detailed execution notes.
</success_criteria>

<output>
Already created: `.planning/phases/03-read-view/03-01-SUMMARY.md`
</output>